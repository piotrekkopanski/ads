-# frozen_string_literal: true
= simple_form_for(@notification) do |f|
  = f.error_notification

  .form-inputs
    = f.input :description
    = f.input :category, collection: ["awaria", "uszkodzenie", "usterka"]
    = f.input :email

  .form-actions
    = f.button :submit
